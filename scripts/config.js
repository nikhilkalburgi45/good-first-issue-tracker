// scripts/config.js

export const USER_PREFERENCES = {
  difficulty: "both", // "beginner" | "intermediate" | "both"
  preferredTags: ["node", "express", "docker", "backend"],
  minStars: 10000,
  maxRepos: 10,
  topIssuesLimit: 5,
};
